spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/series_tracker_dev?currentSchema=series_tracker
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    io.github.nikoir.series.tracker.telegram: DEBUG
    io.github.nikoir.series.tracker.strategy.context: DEBUG
    io.github.nikoir.series.tracker.service: DEBUG